<Window x:Class="WpfApp2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp2"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" 
                   Text="О книге" FontSize="16" FontWeight="Bold" 
                   Margin="0,0,0,10" HorizontalAlignment="Center"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Название:" 
                   Margin="0,0,5,5" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="TitleTextBox" 
                 Margin="0,0,0,5" Text=""
                 TextChanged="TitleTextBox_TextChanged"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Страниц:" 
                   Margin="0,0,5,5" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="PageCountTextBox" 
                 Margin="0,0,0,5" Text="" 
                 PreviewTextInput="NumberValidation"
                 TextChanged="PageCountTextBox_TextChanged"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Цена (руб):" 
                   Margin="0,0,5,5" VerticalAlignment="Center"/>
        <TextBox Grid.Row="3" Grid.Column="1" x:Name="PriceTextBox" 
                 Margin="0,0,0,5" Text="" 
                 PreviewTextInput="DecimalValidation"
                 TextChanged="PriceTextBox_TextChanged"/>


        <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" 
                    Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
            <Button x:Name="SaveBookButton" Content="Сохранить книгу" 
                    Margin="5,0" Padding="10,5" Click="SaveBook_Click"/>
            <Button x:Name="IncreasePagesButton" Content="Добавить 10 страниц" 
                    Margin="5,0" Padding="10,5" Click="IncreasePagesButton_Click"/>
            <Button x:Name="ReducePriceButton" Content="Уменьшить цену в 2 раза" 
                    Margin="5,0" Padding="10,5" Click="ReducePriceButton_Click" ToolTip="Уменьшит цену, если количество страниц больше ста"/>
        </StackPanel>

        <Separator Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2" 
                   Margin="0,10" Background="LightGray"/>

        <TextBlock Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2" 
                   Text="Результат:" FontWeight="Bold" Margin="0,0,0,5" Height="40"/>
        <Border Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="2" 
                BorderBrush="LightGray" BorderThickness="1" CornerRadius="5" 
                Background="#FFF5F5F5" Padding="10">
            <TextBlock x:Name="ResultTextBlock" TextWrapping="Wrap" 
                       Text="Здесь будет результат"/>
        </Border>
    </Grid>
</Window>